# Body Case 1 Prompt

Read and apply `supporting-skills/code-implementation-discipline/SKILL.md`. Work only in the supplied `fixture/` directory. API-created notification jobs default a missing `emailEnabled` preference to `false`, but scheduler-created jobs store `undefined`. Make the preference value canonical for every current producer and consumer. Inspect the complete production path before editing, implement the smallest complete change, run focused verification, and report the required decision and reconciliation.

