# Body Case 3 Prompt

Read and apply `supporting-skills/code-implementation-discipline/SKILL.md`. Work only in the supplied `fixture/` directory. Negative invoice amounts render as `$-12.50`; finance requires `-$12.50` everywhere invoices are presented. Trace the production path, make the smallest complete change at the actual owner even if the observation is already local, run focused verification, and report why the chosen location owns the behavior.

