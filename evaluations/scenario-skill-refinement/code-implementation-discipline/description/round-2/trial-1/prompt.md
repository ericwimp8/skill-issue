# Trial 1 Prompt

Work only in the supplied `fixture/` directory. The support dashboard shows redacted webhook credentials, but the audit export still writes the complete credential. Fix the underlying bug so every current representation receives the same safe value. Inspect the production path before editing, make the smallest complete change, run the focused checks, and report the path you traced, the implementation location you chose, and the verification result.

