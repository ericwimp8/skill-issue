# Trial 2 Prompt

Work only in the supplied `fixture/` directory. Product cancellation events created through the HTTP route include a reason, while cancellations initiated by scheduled expiry omit it. Implement consistent cancellation reasons for both paths. Trace how cancellation is produced and consumed, make the smallest complete change in the appropriate layer, run the focused checks, and summarize the affected paths and verification.

