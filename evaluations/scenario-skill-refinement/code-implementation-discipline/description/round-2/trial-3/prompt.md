# Trial 3 Prompt

Work only in the supplied `fixture/` directory. Invoice totals displayed by the API are rounded to two decimals, but the stored ledger total retains floating-point noise and later reconciliation fails. Correct the behavior for all consumers. Inspect the concrete calculation and storage path first, implement the smallest complete fix, run the focused checks, and report why the changed location owns the behavior.

