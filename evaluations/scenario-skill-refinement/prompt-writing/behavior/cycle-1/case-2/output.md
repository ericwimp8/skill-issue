Read and follow `scripts/release/README.md`, then update `plans/release.md` so its deployment step follows that canonical workflow.

You may edit the plan and run Markdown formatting validation. Request approval before executing any deployment command, and stop before committing or pushing changes.

Return the changed file and the validation result.
