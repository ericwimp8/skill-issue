Create an authoritative, executor-ready implementation plan for adding an export command to the existing CLI.

Read `research/export-command.md` and preserve it as evidence rather than re-summarizing it. Trace the relevant current production paths end to end, verify the research against the repository source, and resolve any discrepancies in favor of current production behavior.

Write only the implementation plan under `plans/`. The plan must give an executor the ordered, source-grounded work needed to implement the command, including the responsible production locations, dependencies, behavior to preserve or add, and validation needed to establish completion. Record any uncertainty that cannot be resolved from the available source.

Do not implement code, alter tests, commit, or push.
