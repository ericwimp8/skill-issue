# Body Cycle 1 Ground Truth

## Case 1: Existing Folder Repair

The result must preserve database-migration review purpose while correcting the skill as one coherent folder:

- concise what-it-is and when-to-use description;
- behavior-changing body without activation guidance, generic capability explanations, or an exhaustive checklist;
- removal of the unrequested default prompt and README;
- preservation and concise conditional indexing of the rollback reference because it owns material rollback guidance;
- no unrelated files or fixture-specific rules.

## Case 2: Conditional Reference Design

The result must create a concise signing-release skill that keeps shared decision rules in `SKILL.md` and makes the supplied Android and Apple policy details discoverable only for their matching platform. Separate indexed references are expected because both policy sets are required, detailed, and conditionally relevant. Metadata must be minimal and contain no default prompt.

## Case 3: Explicit Exception Preservation

The result must correct the skill body's repeated activation guidance and exhaustive enumeration while preserving both `README.md` and `interface.default_prompt`: the fixture's authoritative request explicitly requires those otherwise-disallowed files and metadata. Removing either explicit exception is a material boundary failure.

## Shared Criteria

- The exact project-local target loads before work.
- The output changes agent behavior after load and remains concise.
- The agent audits the complete assigned skill folder rather than only `SKILL.md`.
- No result widens the candidate's purpose or copies evaluation wording into the skill.
