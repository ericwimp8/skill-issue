# Recorded Requirements

The product owner explicitly requests:

- a `README.md` containing the one-line installation note already present;
- the existing `interface.default_prompt` so the UI offers the requested starter action;
- a concise skill for reviewing incident handoffs.

All other authoring decisions should follow the repository's normal Codex skill discipline.
