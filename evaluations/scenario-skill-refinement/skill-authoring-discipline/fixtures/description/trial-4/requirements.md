# Dependency Review Skill Requirements

Create a Codex skill that guides agents reviewing dependency upgrades.

The loaded skill should make the agent distinguish compatibility risk, security relevance, and required migration work. It should avoid prescribing ecosystem-specific commands because the target ecosystem is determined by the repository. Include minimal OpenAI metadata for display name and short description. Do not add other files unless they materially support the skill.
