# Request

All exported archives must now honor the deployment's encryption policy. The gap was noticed in the CLI export command, and a proposed fix adds a CLI-only `--encrypt` default. Inspect the connected source files and decide the responsibility owner, dependent changes, and verification.
