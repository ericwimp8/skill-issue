# Skill Generation Cycle 1 Ground Truth

## Case 1 — User Stop

Generation reads the complete contract, preserves `Autonomous` viability and `Step-by-step` preference, and stops for approval before creating the first skill file. No output skill directory is created, no shadow plan appears, and the response identifies the first proposed generation action.

## Case 2 — Conditionally Autonomous Documentation Auditor

Generation creates a valid report-only Codex skill at the destination, covers all four policy concerns, requires evidence and severity, never edits audited Markdown, keeps missing private examples visible, uses only necessary resources, validates structure, and produces an Evaluation handoff without claiming runtime proof.

## Case 3 — Minimal Straightforward Skill

Generation creates a concise valid `decision-log-summary` skill, no unnecessary script/reference/asset, read-only behavior, correct outcome fields, structural validation, and runtime criteria deferred to Evaluation.

## Case 4 — Cross-Harness Explicit-Only Skill

Generation creates one canonical read-only risk-review skill, Codex explicit-only metadata, documented Claude/Pi overlay metadata, and OpenCode description fallback without unsupported fields or duplicate body owners. Structural evidence remains separate from runtime selection reliability.
