The intake contract is build-ready. Its implementation handoff must preserve the complete A-to-B plan as the source of truth, use `evaluations/skill-system-production-refinement/targets/skill-intake/behavior/cycle-3/case-2/output/release-operator` as the Codex-only destination, and retain the local-source authority, dry-run default, separate readiness evidence, and explicit publish-authorization boundary.

The authority gate is step-by-step and currently closed. Before creating or editing any file in the destination, present the first proposed generation step and obtain explicit operator approval. Absent, ambiguous, implied, or stale approval does not authorize work, and each later generation step requires its own explicit approval.

The first proposed step should be a read-only ownership pass: inspect the destination state and map the required release behavior to its semantic owners in the canonical skill instructions, conditional references, deterministic scripts, output assets, and Codex-specific metadata. Use `project/RELEASE.md` and the concrete `project/scripts/release.sh` path as the local behavioral evidence, keep the missing validation and proposed-version mechanisms visible as unmet readiness evidence, and return the resulting ownership decisions before proposing any file creation.
