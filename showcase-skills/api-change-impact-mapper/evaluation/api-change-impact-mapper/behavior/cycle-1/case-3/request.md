# Behavior Case 3 Request

Analyze the proposed public library interface change in this isolated fixture and produce the complete migration impact report. Trace past the re-export to concrete behavior and cover internal callers, adapters, generated documentation, backend configuration, package consumers, validation, staged publication, and rollback. Keep the work read-only.

Fixture: `showcase-skills/api-change-impact-mapper/evaluation/api-change-impact-mapper/fixtures/library-contract/`
