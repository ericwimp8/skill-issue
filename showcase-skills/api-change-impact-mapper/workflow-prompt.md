Create a reusable skill named `api-change-impact-mapper` by running the complete project-local workflow from `plugins/skill-issue/skills/skill-intake/SKILL.md` through `plugins/skill-issue/skills/skill-generation/SKILL.md` and `plugins/skill-issue/skills/skill-evaluation-and-refinement/SKILL.md`, applying the supporting disciplines those sources require.

The skill must analyze a proposed or observed API, schema, event, or public-interface change against supplied old and new contracts and a repository. It must trace concrete producers, consumers, adapters, generated clients, persisted data, configuration, and external boundaries; classify compatible, conditionally compatible, and breaking impacts; preserve unknown consumers and evidence limits; and produce an actionable migration map with owners, ordering, validation, rollout, and rollback considerations without modifying the implementation.

Use `showcase-skills/api-change-impact-mapper/` as the repository-relative destination. Retain all governed workflow artifacts there. Use synthetic or repository-owned fixtures and authoritative public sources. Create scripts, references, or assets only when genuinely required. Use fresh independent GPT-5.6 Sol agents with medium reasoning for trials. Run required structural, formatting, diff, hash, and privacy checks.
