# Evaluation Prompt

Identity: `dep_body_2_retry`

Work only in `showcase-skills/dependency-upgrade-planner/evaluation/dependency-upgrade-planner/behavior/cycle-1/case-2/`.

Read completely and follow `showcase-skills/dependency-upgrade-planner/skill/dependency-upgrade-planner/SKILL.md`. Create a dependency-ordered, planning-only migration plan for upgrading the isolated `fixtures/vite-library` project from Vite 5.4 to Vite 6. Use only that fixture, `fixtures/authoritative-sources.md`, and the official Vite 6 migration and release pages linked by the ledger. Do not browse beyond those routes, read ground truth or other trial outputs, alter fixture or dependency state, or run broad validation.

Create `prompt.md`, `native-evidence.log`, `output.md`, and `record.md`. Record identity `dep_body_2_retry`, model `gpt-5.6-sol`, reasoning `medium`, the complete-read target hash, inspected repository-relative files and URLs, an eight-criterion audit, cleanup status, and PASS/FAIL.
