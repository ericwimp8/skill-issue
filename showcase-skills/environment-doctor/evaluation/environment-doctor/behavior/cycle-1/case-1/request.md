# Body Case 1 Request

Using the complete `environment-doctor` target, execute a clean deterministic inspection twice against the synthetic fixture. Use child-only PATH entries for primary then secondary, set the synthetic selected variable, inspect `node` and `mystery-tool`, verify primary-before-secondary, and compare `.node-version`. Retain both output pairs, byte-comparison evidence, hashes, a concise audit, and native commands under this case only. Do not modify the target or fixture.
