# Body Case 2 Request

Using the complete `environment-doctor` target, execute one warning/error inspection against the synthetic fixture. With child-only primary-then-secondary PATH, inspect `absent-tool`, keep `INTENTIONALLY_UNSET` unset, check the requested secondary-before-primary relationship, and compare Node with `mismatch.node-version`. Retain the complete output, exit evidence, hashes, concise audit, and native commands under this case only. Preserve every unavailable, reversed, and mismatch state and do not modify the target or fixture.
