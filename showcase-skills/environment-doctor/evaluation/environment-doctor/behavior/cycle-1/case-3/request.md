# Body Case 3 Request

Using the complete `environment-doctor` target and its direct validation harness, independently execute and audit the deterministic, source-preservation, collision, inside-root, traversal, unsupported-tool, privacy-normalization, and caller-environment checks. Retain the validation result, exact hashes, concise audit, and native commands under this case only. Do not change the target, script, harness, or fixtures.
