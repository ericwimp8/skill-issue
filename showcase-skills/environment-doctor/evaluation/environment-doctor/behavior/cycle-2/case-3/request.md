# Body Cycle 2 Case 3 Request

Using the unchanged complete `environment-doctor` target and the refined direct validation harness, independently execute and audit determinism, source and caller-environment preservation, collisions, inside-root output, version-file traversal, malformed tool and environment selectors, unsupported-tool non-execution, literal-home and temporary-root normalization, and synthetic-value omission. Retain only `audit.md` and `native-evidence.log` under this case. Do not change the target, script, harness, or fixtures.
