# Request

A teammate sees different Node versions depending on shell startup order. Diagnose the synthetic development root at `showcase-skills/environment-doctor/fixtures/script/root/` without changing it. For the diagnostic child process only, prepend that root's `toolchain-primary/bin` and `toolchain-secondary/bin` to the existing PATH in that order and set `DIAGNOSTIC_SECRET` to the synthetic value `trial-only-value`. Inspect `node` and `mystery-tool`, confirm the intended directory precedence, check only whether `DIAGNOSTIC_SECRET` and `PATH` are set, and compare Node with `.node-version`. Put the diagnostic artifacts in this trial's `output/` directory, explain the findings and unknowns, and propose only safe remediation and verification steps that require approval before any environment change.
