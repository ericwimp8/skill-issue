# Request

Create a defensible incident record from `fixtures/behavior/case-1/`. Normalize times to UTC, retain all provenance, and identify the next evidence needed to assess the suspected deployment relationship. Write `behavior/cycle-1/case-1/package.md` and derived data in the case directory.
