# Release Contract

Version `7.3.0` at revision `ready-730` is releasable when the local verification command passes, no migration is present, the privacy sample is anonymous, operator notes match the version, and rollback evidence names the previous artifact. Reviewers must not run `deploy.sh`.
