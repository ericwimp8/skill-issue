# Release Contract

Candidate version, application schema, and migration target must match. Destructive migrations require a tested recovery command. Current unit verification must pass. A failed critical gate means not ready.
