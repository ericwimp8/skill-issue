# Release Contract

The exact revision must have current unit, security, and package checks. Security checks require the controlled scanner environment. Readiness cannot be asserted while a required current check is unavailable. Reviews must not request credentials or upload source.
