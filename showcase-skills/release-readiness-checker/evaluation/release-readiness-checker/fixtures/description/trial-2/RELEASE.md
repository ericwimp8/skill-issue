# Release Rules

Required gates are a successful current build, a matching package version, removal of customer identifiers from distributed files, and rollback instructions. Any privacy-gate failure blocks release.
