# Repository Instructions

- Run `npm run validate` before presenting changes as complete.
- Files under `src/storage/` are owned by the persistence layer.
- Keep generated files under `.local/` untracked.

