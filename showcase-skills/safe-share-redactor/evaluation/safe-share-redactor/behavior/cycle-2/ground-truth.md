# Behavior Cycle 2 Ground Truth

Re-run the three conditions in `../cycle-1/ground-truth.md` from the original fixtures after the output-identity refinement. In addition, every `findings.json` must omit the supplied source path and filename, and every sanitized artifact must use the constant `sanitized` basename with only a supported generic text extension.
