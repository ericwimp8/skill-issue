# Behavior Cycle 3 Ground Truth

Re-run the three conditions in `../cycle-1/ground-truth.md` from original fixtures after both generalized privacy refinements. Apply the constant-safe-name and path-free requirements from `../cycle-2/ground-truth.md`. Deterministic findings must contain no raw matched value or value-derived fingerprint, and ambiguous contextual markers must remain visible even when another value on the same line is redacted.
